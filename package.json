{
  "private": true,
  "repository": "git@github.com:block65/eslint-config.git",
  "scripts": {
    "clean": "lerna clean",
    "test": "lerna run test --stream",
    "test:watch": "lerna run test:watch --stream"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
