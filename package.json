{
  "private": true,
  "repository": "git@github.com:block65/eslint-config.git",
  "workspaces": {
    "packages": [
      "./packages/*"
    ]
  },
  "scripts": {
    "clean": "lerna clean",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^5.1.6"
  }
}
